const buildControl = (props) => (
    <div>
        <div>
            {props.label}
            <button>Less</button>
            <button>More</button>
        </div>
    </div>
)

export default buildControl;